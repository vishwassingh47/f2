package com.example.sahil.f2.UiClasses;

/**
 * Created by hit4man47 on 12/28/2017.
 */

public class BottomDialogsTutorial
{



    /*
            View view=mainActivity.getLayoutInflater().inflate(R.layout.layoutof_fast_download,null);
            dialog.setContentView(view);
            dialog.show();

            CoordinatorLayout.LayoutParams params=(CoordinatorLayout.LayoutParams) ((View) view.getParent()).getLayoutParams();
            CoordinatorLayout.Behavior behavior=params.getBehavior();
            BottomSheetBehavior.BottomSheetCallback bottomSheetCallback=new BottomSheetBehavior.BottomSheetCallback() {
                @Override
                public void onStateChanged(@NonNull View bottomSheet, int newState)
                {
                    switch (newState)
                    {
                        case BottomSheetBehavior.STATE_COLLAPSED:
                            Log.e("Bottomdialog","STATE_COLLAPSED");
                            break;
                        case BottomSheetBehavior.STATE_EXPANDED:
                            Log.e("Bottomdialog","STATE_EXPANDED");
                            break;
                        case BottomSheetBehavior.STATE_HIDDEN:
                            Log.e("Bottomdialog","STATE_HIDDEN");
                            break;
                        case BottomSheetBehavior.STATE_DRAGGING:
                            Log.e("Bottomdialog","STATE_DRAGGING");
                            break;
                    }
                }

                @Override
                public void onSlide(@NonNull View bottomSheet, float slideOffset)
                {
                   Log.e("Bottomdialog","slided"+slideOffset);
                }
            };

            if(behavior!=null && behavior instanceof BottomSheetBehavior)
            {
                ((BottomSheetBehavior) behavior).setBottomSheetCallback(bottomSheetCallback);
                Log.e("--","------c-c--c-c");
                ((BottomSheetBehavior) behavior).setPeekHeight(500);
            }
     */




}

